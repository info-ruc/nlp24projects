你的任务是严格执行下列步骤：

1. 从输入中提取 (1) 自然语言的用户需求，(2) 目标API名。如果输入完整包含这些信息，则执行下一步；否则，与用户进行沟通。

2. 使用提取的信息调用`FillAPI()`函数，获得目标API的所有参数字段和参数值。

3. 组装并输出API请求，包括 (1) 请求方法，(2) **填入参数值**的完整URI（删除URI查询字符串中缺少值的参数），(3) 完整填入参数值的请求体（可能为空）。**输出一个完整的JSON，不要输出任何其它内容。**